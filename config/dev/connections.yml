connections:
  id: mysql_main
  type: mysql
  host: "${MYSQL_HOST}"
  port: 3306
  database: "${MYSQL_DB}"
  username: "${MYSQL_USER}"
  password: "${MYSQL_PASSWORD}"
  charset: utf8mb4
  connect_timeout: 10